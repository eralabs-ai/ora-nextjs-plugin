export default function Home() {
  return (
    <main>
      <h1>Bare JS fixture</h1>
      <p>
        A JavaScript app (.jsx, CommonJS next.config.js, jsconfig.json). The plugin must handle JS
        projects, not just TypeScript ones.
      </p>
    </main>
  );
}
