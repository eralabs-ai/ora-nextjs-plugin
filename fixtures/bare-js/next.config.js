// CommonJS next.config.js (no "type": "module" in package.json). The CLI must read this format too,
// alongside .mjs and .ts.
/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
