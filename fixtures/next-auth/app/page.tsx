export default function Home() {
  return (
    <main>
      <h1>next-auth fixture</h1>
      <p>
        Human sign-in lives at /api/auth/* via next-auth. Nothing else here — the fixture exists to
        prove provider detection against a real dependency in a real build.
      </p>
    </main>
  );
}
