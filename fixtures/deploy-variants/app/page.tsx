export default function Home() {
  return (
    <main>
      <h1>Deploy variants fixture</h1>
      <p>
        basePath is /app and output is standalone. The CLI reads these from next.config and chooses
        the right emission target.
      </p>
    </main>
  );
}
