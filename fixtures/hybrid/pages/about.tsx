export default function About() {
  return (
    <main>
      <h1>About (Pages Router)</h1>
      <p>The `/about` route, served by the Pages Router — unioned into the same route table.</p>
    </main>
  );
}
