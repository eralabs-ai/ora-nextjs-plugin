export default function Home() {
  return (
    <main>
      <h1>MCP adapter fixture</h1>
      <p>
        An existing MCP server at /[transport] — unambiguous intent to publish, so zero-config
        should include it.
      </p>
    </main>
  );
}
