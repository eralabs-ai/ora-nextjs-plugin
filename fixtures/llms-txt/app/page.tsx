export default function Home() {
  return (
    <main>
      <h1>llms.txt fixture</h1>
      <p>
        This app serves /llms.txt from a route handler (the recommended Next.js way). The plugin
        detects it and references it as a text/markdown entry — a confirmed-ingested artifact.
      </p>
    </main>
  );
}
