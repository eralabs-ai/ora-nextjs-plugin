export default function Home() {
  return (
    <main>
      <h1>Bare fixture</h1>
      <p>Zero-config baseline: the plugin should publish only unambiguous site-level metadata.</p>
    </main>
  );
}
