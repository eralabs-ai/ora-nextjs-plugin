# fixture: bare

A fresh `create-next-app` (App Router) with **no config and no tools**.

**Exercises:** the zero-config path. The plugin should emit only unambiguous site-level metadata
(name, description) and **no** route-level tool entries. This is also the Phase 1 walking-skeleton
target: the fixture whose deployed catalog gets run through journey.ora.ai.
