export default function Home() {
  return (
    <main>
      <h1>MDX content fixture</h1>
      <p>
        An ordinary prerendered page living beside the MDX guides, proving Tier-1 (source-derived)
        and Tier-2 (build-output-derived) twins coexist in one build without stepping on each other.
        Its own twin is derived from this prerendered HTML, since no markdown source covers this
        route.
      </p>
    </main>
  );
}
