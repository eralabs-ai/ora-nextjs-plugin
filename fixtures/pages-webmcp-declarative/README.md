# pages-webmcp-declarative

A **Pages Router** app with a declarative WebMCP `<form toolname>` on `pages/index.tsx`. It proves
declarative WebMCP detection attributes the tool to the right page URL (`/`) via the Pages Router
file-is-the-route rule, emitting a `text/html` catalog entry just as the App Router variant does.
