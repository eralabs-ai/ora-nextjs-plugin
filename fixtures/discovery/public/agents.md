# Agent guidance

## When to use

- Use this site's catalog to discover its OpenAPI doc and MCP server before calling anything.

## When NOT to use

- Do not scrape the HTML UI for data that the API already exposes in a structured form.
