export default function Home() {
  return (
    <main>
      <h1>Edge cases fixture</h1>
      <p>
        Adversarial patterns: dynamic schemas, conditional registration, registerTool in a server
        module, and a decoy user-defined registerTool. The plugin must handle these without emitting
        false positives.
      </p>
    </main>
  );
}
